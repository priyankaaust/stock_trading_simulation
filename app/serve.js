
/**
 * 
 * Modified by Team J
 **/ 

import app from './app.js';
const port = 3000;

app.listen(port, () => {
    console.log(`Stocks Simulator listening at http://localhost:${port}`);
});

export default app;
